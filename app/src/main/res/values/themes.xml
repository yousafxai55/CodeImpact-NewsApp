<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Light Mode -->
    <style name="Base.Theme.LatestNews" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your light theme here -->
        <item name="android:windowBackground">@color/white</item>
        <item name="colorPrimaryDark">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item> <!-- Ensures dark icons on status bar -->
        <!-- <item name="android:navigationBarColor">@color/white</item> -->
    </style>

    <style name="Theme.LatestNews" parent="Base.Theme.LatestNews" />

    <style name="Theme.LatestNews.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.ActionBar" />

    <style name="Theme.LatestNews.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light" />

    <!-- Light theme with ActionBar (if needed) -->
    <style name="Theme.LatestNews.Light" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- No need to redefine statusBarColor here, it inherits from Base.Theme.LatestNews -->
        <item name="android:windowBackground">@color/white</item>
        <item name="android:statusBarColor" tools:targetApi="l">@color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>


    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>